{
  "name": "salang-classroom-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
